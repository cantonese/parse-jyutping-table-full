# @cantonese/parse-jyutping-table-full

This is a helper script which parses `JPTableFull.pdf` into a usable dataset. `JPTableFull.pdf` contains Hong Kong's contribution to ISO 10646.

This is a `node` script.

`node ./`
